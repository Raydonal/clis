# Perturbation matrix for the case-weights scheme

Perturbation matrix for the case-weights scheme

## Usage

``` r
delta_caseweights(object)
```

## Arguments

- object:

  A fitted BEZI/BEOI `gamlss` model.

## Value

A list with the full `Delta` matrix and its `gamma`, `beta`, `delta`
blocks.
