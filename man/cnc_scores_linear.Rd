% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnc.R
\name{cnc_scores_linear}
\alias{cnc_scores_linear}
\title{Linear-time per-observation CNC scores}
\usage{
cnc_scores_linear(Delta, info_inv)
}
\arguments{
\item{Delta}{A perturbation matrix of dimension \verb{(M+m+p) x n}.}

\item{info_inv}{The inverse information matrix from \code{\link[=bic_info]{bic_info()}}.}
}
\value{
A list with per-observation scores \code{B_Et}, the cutoff \code{b2}, the
Frobenius norm \code{normF}, and \code{n}.
}
\description{
Computes the basic-perturbation CNC scores \eqn{B_{E_t}} without ever
forming the \eqn{n \times n} curvature matrix. The score is the scaled
diagonal of \eqn{F_0 = \Delta^\top \mathcal{I}^{-1}\Delta}, and both the
diagonal and the Frobenius norm \eqn{\|F_0\|_F} are obtained from
quantities of dimension \eqn{(M+m+p)}, so the cost is linear in \code{n} and
the memory footprint is negligible. This is the scalable path used for
large samples, where the dense \eqn{n\times n} eigenproblem of
\code{\link[=cnc_matrix]{cnc_matrix()}} is infeasible.
}
