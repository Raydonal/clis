% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bic-core.R
\name{bic_info}
\alias{bic_info}
\title{Observed or Fisher information matrix for a BIc model}
\usage{
bic_info(object, use_fisher = TRUE, penalty = NULL)
}
\arguments{
\item{object}{A fitted \code{gamlss} model of family \code{BEZI} or \code{BEOI}.}

\item{use_fisher}{Logical; if \code{TRUE} (default) returns the expected
(Fisher) information, which is guaranteed positive definite; if \code{FALSE}
returns the observed information.}

\item{penalty}{Optional penalty matrix \code{S} of dimension
\verb{(M+m+p) x (M+m+p)} for penalised additive submodels. When supplied,
the returned information is the penalised information \code{J + S}
(or \code{I + S}), following the semiparametric extension. The penalty
must be block diagonal across the \code{gamma}, \code{beta}, \code{delta} groups so
that separability is preserved; see \code{\link[=bic_penalty]{bic_penalty()}}.}
}
\value{
A list with the information matrix (\code{info}), its inverse
(\code{info_inv}), the parameter-block indices (\code{idx}), the weight
sequences (\code{weights}), and (if a penalty was supplied) the effective
degrees of freedom (\code{edf}) and their block split (\code{edf_blocks}).
}
\description{
Computes the block-structured information matrix of a fitted zero-or-one
inflated beta regression with variable dispersion. By construction the
matrix is block diagonal between the inflation parameters and the
mean/precision parameters (information orthogonality).
}
\references{
Ospina, R. and Ferrari, S. L. P. (2012). A general class of zero-or-one
inflated beta regression models. \emph{Computational Statistics & Data
Analysis}, 56(6), 1609-1623.
}
