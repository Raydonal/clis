% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bic-core.R
\name{bic_penalty}
\alias{bic_penalty}
\title{Extract the penalty matrix from a fitted additive BIc model}
\usage{
bic_penalty(object)
}
\arguments{
\item{object}{A fitted \code{gamlss} model of family \code{BEZI} or \code{BEOI} with
penalised additive terms in one or more submodels.}
}
\value{
A block-diagonal penalty matrix of dimension
\verb{(M+m+p) x (M+m+p)}, block diagonal across the inflation, mean, and
precision coefficient groups.
}
\description{
Assembles the block-diagonal penalty matrix \code{S(lambda)} from the smooth
terms of a fitted \code{gamlss} model whose submodels use penalised additive
terms (for example \code{pb()} P-spline terms). The result is suitable as the
\code{penalty} argument of \code{\link[=bic_info]{bic_info()}}.
}
\details{
The function reads the smoothing structure stored by \code{gamlss}
for each parameter (\code{mu}, \code{sigma}, \code{nu}) and places the corresponding
penalty contributions on the diagonal blocks. Terms without a penalty
contribute a zero block, so a model with a mix of linear and smooth
terms is handled transparently. By construction the returned matrix is
block diagonal across the three coefficient groups, so it preserves the
separability required by the semiparametric theory.
}
\seealso{
\code{\link[=bic_info]{bic_info()}}
}
