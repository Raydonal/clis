% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cnc.R
\name{cnc_scores}
\alias{cnc_scores}
\title{Per-observation conformal normal curvature scores}
\usage{
cnc_scores(cnc, r_max = 4L)
}
\arguments{
\item{cnc}{A list returned by \code{\link[=cnc_matrix]{cnc_matrix()}}.}

\item{r_max}{Integer; the maximum order of \code{r}-influential eigenvectors to
consider for the aggregate contributions.}
}
\value{
A list with per-observation scores \code{B_Et}, the matrix of aggregate
contributions \code{m_r} (one column per \code{r}), the eigenvalue thresholds
\code{threshold_r}, the aggregate-contribution thresholds \code{threshold_mt}, the
number of \code{r}-influential eigenvectors \code{k_r}, and the cutoff \code{b2} for
\code{B_Et}.
}
\description{
Computes the basic-perturbation CNC scores \eqn{B_{E_t}} and the aggregate
contribution measures \eqn{m[r]_t} from a fitted CNC matrix.
}
